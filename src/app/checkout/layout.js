export const metadata = {
  title: 'Checkout',
  description: 'Complete your order — secure checkout for Vault 6 Studios.',
  robots: { index: false, follow: false },
};

export default function CheckoutLayout({ children }) {
  return children;
}
