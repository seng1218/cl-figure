export const metadata = {
  title: 'Vault Ops',
  description: 'Admin dashboard — Vault 6 Studios.',
  robots: { index: false, follow: false },
};

export default function VaultOpsLayout({ children }) {
  return children;
}
